const InitialState = {
  test: 'hello world'
};

const actionTypes = {};

// REDUCERS
const reducer = (state = InitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
